name: Autonomous Market Creator

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "Optional topic; leave blank to let the AI choose the strongest live market opportunity"
        required: false
        default: ""
  schedule:
    - cron: "17 */6 * * *"

permissions:
  contents: write

concurrency:
  group: autonomous-market-creator
  cancel-in-progress: true

jobs:
  create-draft:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --disable-pip-version-check "google-genai>=1.0.0"

      - name: Scan live Binance market
        run: python src/live_market_scanner.py

      - name: Run autonomous multi-agent creator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-3.6-flash
          TOPIC: ${{ inputs.topic }}
        run: python src/multi_agent_creator.py

      - name: Save market snapshot and draft
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/live/ data/reports/
          git diff --cached --quiet && exit 0
          git commit -m "Add autonomous market research and draft"
          git push
